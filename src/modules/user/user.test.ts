import 'jest';

describe("Calculator Tests", () => {
    test("Addition of 2 numbers", () => {
        // arrange and act
        var result = 3

        // assert
        expect(result).toBe(3);
    });

})